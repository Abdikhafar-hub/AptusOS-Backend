DO $$ BEGIN
  CREATE TYPE "BudgetType" AS ENUM ('ANNUAL', 'QUARTERLY', 'MONTHLY', 'PROJECT_BASED');
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

DO $$ BEGIN
  CREATE TYPE "BudgetCategory" AS ENUM ('OPERATIONAL', 'CAPEX', 'PAYROLL', 'MARKETING', 'IT', 'OTHER');
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

DO $$ BEGIN
  CREATE TYPE "BudgetApprovalStatus" AS ENUM ('DRAFT', 'PENDING_APPROVAL', 'APPROVED');
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

DO $$ BEGIN
  CREATE TYPE "OverspendingLimitType" AS ENUM ('PERCENT', 'AMOUNT');
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

ALTER TABLE "Budget"
ADD COLUMN IF NOT EXISTS "name" TEXT,
ADD COLUMN IF NOT EXISTS "budgetType" "BudgetType" NOT NULL DEFAULT 'ANNUAL',
ADD COLUMN IF NOT EXISTS "budgetCategory" "BudgetCategory" NOT NULL DEFAULT 'OPERATIONAL',
ADD COLUMN IF NOT EXISTS "periodStartDate" TIMESTAMP(3),
ADD COLUMN IF NOT EXISTS "periodEndDate" TIMESTAMP(3),
ADD COLUMN IF NOT EXISTS "lineItems" JSONB,
ADD COLUMN IF NOT EXISTS "approvalStatus" "BudgetApprovalStatus" NOT NULL DEFAULT 'DRAFT',
ADD COLUMN IF NOT EXISTS "budgetOwnerId" TEXT,
ADD COLUMN IF NOT EXISTS "approverId" TEXT,
ADD COLUMN IF NOT EXISTS "approvedById" TEXT,
ADD COLUMN IF NOT EXISTS "approvedAt" TIMESTAMP(3),
ADD COLUMN IF NOT EXISTS "createdById" TEXT,
ADD COLUMN IF NOT EXISTS "allowOverspending" BOOLEAN NOT NULL DEFAULT false,
ADD COLUMN IF NOT EXISTS "overspendingLimitType" "OverspendingLimitType",
ADD COLUMN IF NOT EXISTS "overspendingLimitValue" DECIMAL(65,30),
ADD COLUMN IF NOT EXISTS "alertThresholds" INTEGER[] DEFAULT ARRAY[50, 75, 90, 100]::INTEGER[],
ADD COLUMN IF NOT EXISTS "costCenter" TEXT,
ADD COLUMN IF NOT EXISTS "projectCode" TEXT,
ADD COLUMN IF NOT EXISTS "notes" TEXT,
ADD COLUMN IF NOT EXISTS "supportingDocumentId" TEXT,
ADD COLUMN IF NOT EXISTS "amountChangeHistory" JSONB;
